extends ../layout.pug

block title
  | #{status} &laquo; #{detail}

block content
  section.errorsect
    span.status= status
    span.detail= detail
  if err
    section
      .code
        pre= JSON.stringify(err, null, 2)
