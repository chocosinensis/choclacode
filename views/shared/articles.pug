mixin articles(articles, search)
  if search
    input#search(type="text", placeholder="Enter Search Term")
  ul.articles
    for article in articles
      li.article.link
        a(href=`/articles/${article.slug}`)
          h1.title= article.title
          small.likes= article.likes.length ? `${article.likes.length} ${article.likes.length == 1 ? 'like' : 'likes'}` : ''
          .author.title @#{ article.author.name }
          .time= article.createdAt
